// Types and constants for the app. Runtime results come only from /api/results
// after a live harness run — the UI never bootstraps from saved JSON.

export type { ModelId, ModelResult, RunStep, Site } from "./types";
export { MODELS, MODEL_LABELS } from "./types";
